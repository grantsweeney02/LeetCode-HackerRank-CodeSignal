public static int pageCount(int n, int p) {
    if(p==n || p ==1) {
        return 0;
    }
    if(n-p <= n/2) {
        if(n%2==0 && n-p==1) {
            return ((n-p)/2) +1;
        }
        return ((n-p)/2);
    }
    return (p/2);
}