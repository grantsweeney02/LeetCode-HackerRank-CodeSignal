boolean solution(int n) {
    String nums = String.valueOf(n);
    for (int i = 0; i < nums.length(); i++) {
        if(nums.charAt(i)%2 != 0) {
            return false;
        }
    }
    return true;
}
